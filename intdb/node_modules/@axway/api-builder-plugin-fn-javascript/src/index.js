const path = require('path');
const { SDK } = require('@axway/api-builder-sdk');
const actions = require('./actions');

function loadPlugin() {
	const sdk = new SDK();
	sdk.load(path.join(__dirname, 'flow-nodes.yml'), actions);
	return sdk.getPlugin();
}

module.exports = loadPlugin;
