async function stopBilling() {
	try {
		const billingManager = this._internal.getBillingManager();
		if (billingManager) {
			this.logger.trace('stopping billing manager');
			await billingManager.stop();
		}
	} catch (err) {
		this.logger.error(err);
	}
}

module.exports = stopBilling;
