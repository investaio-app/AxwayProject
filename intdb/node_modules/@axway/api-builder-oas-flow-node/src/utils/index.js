const getParentPluginInfo = require('./getParentPluginInfo');
const generateFunctionName = require('./generateFunctionName');
const sort = require('./sort');
const schema = require('./schema');

module.exports = {
	getParentPluginInfo,
	...generateFunctionName,
	...sort,
	...schema
};
