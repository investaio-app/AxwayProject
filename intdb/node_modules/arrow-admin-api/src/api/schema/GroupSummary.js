class GroupSummary {
	static get schema() {
		return {
			title: 'GroupSummary',
			properties: {
				id: {
					type: 'string'
				},
				name: {
					type: 'string'
				},
				description: {
					type: 'string'
				},
				status: {
					$ref: '#/definitions/Status'
				}
			},
			required: [
				'id',
				'name'
			]
		};
	}

	static validate(groupSummary) {
		let valid = true;
		this.schema.required.forEach(prop => {
			if (!{}.hasOwnProperty.call(groupSummary, prop)) {
				valid = false;
			}
		});
		return valid;
	}
}

module.exports = GroupSummary;
