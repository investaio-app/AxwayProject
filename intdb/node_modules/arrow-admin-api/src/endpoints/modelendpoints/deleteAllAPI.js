module.exports = ({ operationId, model, encodedModelName }) => {
	return {
		swagger: '2.0',
		info: {
			title: `${model.name}`,
			version: '1.0.0'
		},
		paths: {
			[`/${encodedModelName}`]: {
				delete: {
					operationId,
					tags: [
						`${model.name}`
					],
					summary: `Deletes all ${model.name}s`,
					consumes: [],
					produces: [
						'application/json'
					],
					responses: {
						204: {
							description: 'The delete succeeded.'
						},
						404: {
							description: 'No results were found.'
						},
						500: {
							description: 'Something went wrong during the request; check out the logs on your server.'
						}
					}
				}
			}
		}
	};
};
