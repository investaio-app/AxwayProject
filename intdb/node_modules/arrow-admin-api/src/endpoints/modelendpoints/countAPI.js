const { arrowQueryOptions } = require('../../util');

module.exports = ({ operationId, model, encodedModelName }) => {
	return {
		swagger: '2.0',
		info: {
			title: `${model.name}`,
			version: '1.0.0'
		},
		paths: {
			[`/${encodedModelName}/count`]: {
				get: {
					operationId,
					tags: [
						`${model.name}`
					],
					summary: `Count a ${model.name}`,
					consumes: [],
					produces: [
						'application/json'
					],
					parameters: [ ...arrowQueryOptions.filter(opt => opt.name === 'where') ],
					responses: {
						200: {
							description: 'The count succeeded, and the results are available.',
							schema: {
								type: 'integer'
							}
						},
						400: {
							description: 'Bad request.'
						},
						404: {
							description: 'No results were found.'
						},
						500: {
							description: 'Something went wrong during the request; check out the logs on your server.'
						}
					}
				}
			}
		}
	};
};
