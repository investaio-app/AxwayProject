module.exports = ({ operationId, schemaRef, model, encodedModelName }) => {
	return {
		swagger: '2.0',
		info: {
			title: `${model.name}`,
			version: '1.0.0'
		},
		paths: {
			[`/${encodedModelName}`]: {
				get: {
					operationId,
					tags: [
						`${model.name}`
					],
					summary: `Find all ${model.name}`,
					consumes: [],
					produces: [
						'application/json'
					],
					responses: {
						200: {
							description: 'The find all succeeded, and the results are available.',
							schema: {
								type: 'array',
								items: {
									$ref: schemaRef.full
								}
							}
						},
						404: {
							description: 'No results were found.'
						},
						500: {
							description: 'Something went wrong during the request; check out the logs on your server.'
						}
					}
				}
			}
		}
	};
};
