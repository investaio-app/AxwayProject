const fs = require('fs');
const util = require('util');

// Switch to 'fs/promises' when we bump node versions
module.exports = {
	writeFile: util.promisify(fs.writeFile),
	readFile: util.promisify(fs.readFile),
	unlink: util.promisify(fs.unlink)
};
