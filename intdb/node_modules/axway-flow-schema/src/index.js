// FIXME: note these are all invalid draft-06 schemas. (id should be $id)
// There is an issue with axway-schema which requires the id to be "id".
// We should make a breaking change to a few modules which we update all our
// internal schemas to draft 7 (correctly) and stop loading them into
// axway-schema.

const flow = {
	all: require('../flowschema.json'),
	v2: require('../flowschema-v2.json'),
	v3: require('../flowschema-v3.json'),
	v4: require('../flowschema-v4.json')
};

const flownode = {
	all: require('../flownodeschema.json'),
	v1: require('../flownodeschema-v1.json'),
	v2: require('../flownodeschema-v2.json')
};

const all = [
	flow.all,
	flow.v2,
	flow.v3,
	flow.v4,
	flownode.all,
	flownode.v1,
	flownode.v2
];

module.exports = {
	flow,
	flownode,
	all
};
